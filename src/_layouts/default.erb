<!doctype html>
<html lang="<%= site.locale %>" class="js-html">
  <head>
    <%= render "head", metadata: site.metadata, title: resource.data.title %>
  </head>
  <body class="layout-<%= resource.layout.label %> <%= resource.data.page_class %>">
    <a href="#main" class="go-to-content-link">Skip to main content</a>
    <%= render "header" %>

    <%= yield %>

    <%= render "footer", metadata: site.metadata %>
  </body>
</html>
